import React, { Component } from 'react';
import './Logo.css';

function Logo(){
    return (
        <section className="logo">
            <h2 className="logo_title">Wovenly</h2>
        </section>
      );
}

export default Logo;
